import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function CloseWorkdayScreen() {
  return <PlaceholderScreen title={strings.workday.closeTitle} />;
}
