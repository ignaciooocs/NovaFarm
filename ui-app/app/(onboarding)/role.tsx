import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function RoleScreen() {
  return <PlaceholderScreen title={strings.onboarding.roleQuestion} />;
}
