import { Injectable } from '@nestjs/common';

// Servicio de ejemplo generado por el scaffold de Nest — no tiene lógica de
// negocio real, solo respalda la ruta GET /api/v1 de app.controller.ts.
@Injectable()
export class AppService {
  getHello(): string {
    return 'Hello World!';
  }
}
