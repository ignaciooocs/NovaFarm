import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function SyncScreen() {
  return <PlaceholderScreen title={strings.sync.title} />;
}
