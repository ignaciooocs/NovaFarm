import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function JoinFarmScreen() {
  return <PlaceholderScreen title={strings.onboarding.joinFarmTitle} />;
}
