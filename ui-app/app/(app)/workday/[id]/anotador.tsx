import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function AnotadorScreen() {
  return <PlaceholderScreen title={strings.anotador.title} />;
}
