import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function HarvestersScreen() {
  return <PlaceholderScreen title={strings.admin.harvestersTitle} />;
}
