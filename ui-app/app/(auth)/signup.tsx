import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function SignupScreen() {
  return <PlaceholderScreen title={strings.auth.signupTitle} />;
}
