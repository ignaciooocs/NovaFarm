import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { strings } from '@/constants/strings';

export default function CreateFarmScreen() {
  return <PlaceholderScreen title={strings.onboarding.farmNameLabel} />;
}
